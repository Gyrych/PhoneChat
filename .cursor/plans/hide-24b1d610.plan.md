<!-- 24b1d610-fd36-4d61-9e19-a46d88e10d91 2d7b74fd-6f93-4afe-8ced-877f0ef410e5 -->
# 隐藏“清空日志”按钮（主聊页与会话管理页）

## 目标

- 按你的选择：范围为 `index.html` 与 `conversations.html`，方式为“隐藏按钮（便于将来恢复）”。
- 仅隐藏 UI，不删除功能代码；日志导出按钮保留可见。
- 同步更新 `README.md`、`README_zh.md` 与 `CURSOR.md` 的相关描述。

## 变更点（文件级）

- `style.css`：新增选择器隐藏两个页面共享的 `#clearLogsBtn`。
- `README.md` / `README_zh.md`：更新“请求/响应日志”章节与使用说明，说明“清空日志按钮默认隐藏，功能仍保留”。
- `CURSOR.md`：
  - 主体“日志架构与数据流 → UI”从“两个按钮（导出/清空）”改为“默认仅显示导出；清空隐藏（可恢复）”。
  - 追加变更记录条目（日期、目的、修改项）。

## 关键实现片段（新增 CSS）

```css
/* 隐藏清空日志按钮（主聊页与会话管理页共用）；未来需要时可移除此规则恢复可见 */
#clearLogsBtn { display: none !important; }
```

## 不修改项

- 不改动任何 JS 绑定与日志实现（`window.Logger.clear()` 仍可用）。
- 不改动“消息条目级”的删除按钮（仍保留每条消息旁的删除功能）。

## 风险与回退

- 低风险：仅样式层隐藏；若需恢复，删除上述 CSS 规则即可。

### To-dos

- [ ] 在 style.css 添加隐藏 #clearLogsBtn 的 CSS 规则
- [ ] 更新 README.md 和 README_zh.md 对日志按钮的描述
- [ ] 更新 CURSOR.md 主体 UI 描述并追加变更记录